# Optional — copy values into system env, lab-config.json, or a launcher script

CODETRACK_API_URL=http://localhost:3000/api
CODETRACK_SOCKET_URL=http://localhost:3000
CODETRACK_DASHBOARD_URL=http://localhost:5173

# Generic update feed base URL (electron-updater). Leave empty to disable.
# CODETRACK_UPDATE_URL=https://your-host.example/companion-updates/student

# Force role even on unlocked builds: student | professor
# CODETRACK_ROLE=student
